# Core dependencies
numpy>=1.21
pandas>=1.3
torch>=1.10
scikit-learn>=1.0

# Logging (표준 logging은 설치 불필요하지만 YAML 기반 설정 쓸 경우 pyyaml 필요할 수 있음)
pyyaml>=5.4

# Visualization and Progress
matplotlib>=3.4
tqdm>=4.62

# Optimal Transport (UltraRE)
POT>=0.8.0

# Experiment Tracking
wandb>=0.15.0

# PyTorch Geometric & 관련 의존성 (CUDA 11.8 기준)
torch-geometric==2.3.1
torch-scatter==2.1.2
torch-sparse==0.6.18
torch-cluster==1.6.1
torch-spline-conv==1.2.2